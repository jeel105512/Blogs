<%
    let action = "";
    let name = "";
    let method = "post";

    if (typeof application !== "undefined") {
        action = `/${application.id}`;
        name = application.name;
        method = "put";
    }
%>

<form action="/applications<%= action %>" method="post" class="mt-3">
    <input type="hidden" name="_method" value="<%= method %>">

    <div class="mb-3">
        <label for="name" class="form-label text-light">Name:</label>
        <input type="text" class="form-control bg-dark text-light" id="name" name="name" required value="<%= name %>">
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>

    <a href="/applications" class="btn btn-secondary ms-2">Cancel</a>
</form>
